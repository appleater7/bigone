package com.big.one.config;
/*
 * 이것은 메모장
 * #1. 기본 기능 구현 우선
 *     (local db(o), mvc pattern(o), mybatis settings(o), hikari settings(o), encoding filter(o))
 * #2. @valid 구현
 * #3. aop 구현에 따른 메서드 포인트컷 구현
 * 
 * 
 * 
 */
public class Memo {

}
